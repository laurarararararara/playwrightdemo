{
  "name": "playwright-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright 0→1 入门 demo",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "site": "node scripts/serve.js",
    "site:about": "node scripts/serve.js",
    "report": "node scripts/show-latest-log.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
